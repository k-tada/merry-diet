<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |t| %>
    <li>
      <ul>
        <% t.each do |k, v| %>
          <li><%= k %> : <%= v.inspect %></li>
        <% end %>
      </ul>
    </li>
  <% end %>
</ul>

<%= link_to '新規登録', new_task_path %>

